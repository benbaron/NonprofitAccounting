# Alternate UI Migration Inventory

This inventory tracks the current migration state of the alternate JavaFX UI in `org.nonprofitbookkeeping.ui`. It is based on:

- `src/main/java/org/nonprofitbookkeeping/ui/AppPanelId.java`
- `src/main/java/org/nonprofitbookkeeping/ui/PanelHost.java`
- `src/main/java/org/nonprofitbookkeeping/ui/MainWindowAlternate.java`
- currently available legacy panels and actions under `src/main/java/nonprofitbookkeeping/ui/panels` and `src/main/java/nonprofitbookkeeping/ui/actions`

## Status legend

- **Native alternate UI**: an `org.nonprofitbookkeeping.ui` panel or view intended for the alternate shell.
- **Legacy adapter**: an alternate-shell `AppPanel` or direct command that embeds an old `nonprofitbookkeeping.ui` panel.
- **Placeholder**: UI exists but primarily communicates that functionality is not yet implemented or wired.
- **Demo-only**: production UI displays hardcoded realistic business/accounting sample data.
- **Mixed**: combines service-backed behavior with placeholder, demo, direct legacy, or incomplete behavior.
- **Missing**: no first-class alternate panel or workflow exists yet.
- **Dropped/not in scope**: explicitly excluded from the alternate UI migration plan.

## AppPanelId inventory

| AppPanelId / Workflow | Current class or route | Current status | Old UI / existing source to inspect | Missing old functionality / migration notes | Target native panel or workflow | Required services / collaborators | Test status |
|---|---|---|---|---|---|---|---|
| `DASHBOARD` | `MainWindowAlternate` custom dashboard canvas; `PanelHost.DefaultPanelFactory` can also create `FxAppPanelAdapter<>("Dashboard", DashboardPanelFX::new, ...)` | Mixed / demo-only in shell | `src/main/java/nonprofitbookkeeping/ui/panels/DashboardPanelFX.java`; `src/main/java/org/nonprofitbookkeeping/ui/panels/DashboardPanelFX.java` | The shell dashboard currently shows hardcoded receivables, payables, profit/loss, cash-flow chart, and account balance values. Needs real dashboard queries or explicit empty states and consistent routing to one implementation. | `AlternateDashboardPanel` or service-backed alternate dashboard surface | Dashboard data bridge/query services; date range context; active company context | Needed: guard tests that no production demo rows/cards are inserted by default; service-backed/empty-state tests. |
| `LEDGER_REGISTER` | `PanelHost.DefaultPanelFactory` -> `LedgerRegisterPanel` | Native | `JournalPanelFX`; `AccountsActivityPanelFX`; `GeneralJournalEntryPanelFX`; `JournalEntryWorkspaceFX`; `LedgerReconcilePanelFX`; `AccountTransactionDetailsPanelFX` | Native alternate register now uses a service-backed query, renders transaction headers plus journal-entry detail lines, supports filters for date/account/memo/amount/fund/cleared/reconciled state, preserves posted-history delete safety, and exposes import-review entry points. Journal entry New/Edit/Open currently uses a clearly marked temporary adapter to `GeneralJournalEntryPanelFX`. | `LedgerRegisterPanel` with `LedgerTransactionQueryService` | `LedgerTransactionQueryService`; active `CurrentCompany` ledger/journal; temporary `GeneralJournalEntryPanelFX` adapter | Added split-transaction selected-account amount correctness test in `LedgerTransactionQueryServiceTest`; remaining useful coverage: JavaFX interaction tests for filter controls and adapter launch. |
| `SCHEDULES` | `WorkspaceRouter` routes to `MainWindowAlternate.buildAlternateSchedulesPane()` custom pane; `PanelHost.DefaultPanelFactory` also has `SchedulesPanel` but normal routing bypasses it | Mixed / placeholder | Outstanding detail/schedule concepts in older reporting/account panels; scheduled report action in `MainWindowAlternate.openReportsScheduleDirect()` | Custom pane only stores scheduled report strings in preferences. `SchedulesPanel` has schedule tabs and service-backed eligibility but placeholder tab contents and fallback demo accounts. Needs real schedule grids and no fallback demo accounts in production. | `AlternateSchedulesPanel` | Schedule eligibility service; schedule/query services; report scheduling service; account lookup | Needed: migration guard tests for route choice, no fallback demo accounts, and scheduled-report persistence behavior. |
| `INVENTORY` | `PanelHost.DefaultPanelFactory` -> `InventoryPanel` wrapping `InventoryPanelFX` | Legacy adapter | `InventoryPanelFX`; inventory and asset/depreciation services | Native alternate inventory management, transfer/order handling, lifecycle actions, and service-bound empty/error states still need migration. | `AlternateInventoryPanel` | `InventoryService`; inventory transfer/order services; asset/depreciation services where applicable | Needed: adapter coverage now; native inventory service tests when migrated. |
| `BUDGET_EDITOR` | `WorkspaceRouter` routes to `MainWindowAlternate.buildAlternateBudgetEditorPane()` custom pane; `PanelHost.DefaultPanelFactory` also has `BudgetEditorPanel` but normal routing bypasses it | Mixed | `JournalEntryWorkspaceFX`; report/budget-related legacy flows; `GenerateReportPanelFX` report context | Custom pane embeds `JournalEntryWorkspaceFX` when a company is open. `BudgetEditorPanel` contains hardcoded budget rows and does not persist budget data. Needs a real budget model/service-backed editor. | `AlternateBudgetEditorPanel` | Budget service/query model; fund lookup; journal-style validation where appropriate | Needed: service tests once budget behavior exists; UI tests for open-company gating and no demo rows. |
| `BUDGET_VS_ACTUAL` | `PanelHost.DefaultPanelFactory` -> `BudgetVsActualPanel` | Demo-only | `ReportsPanelFX`; `GenerateReportPanelFX`; income/balance/report writer classes | Current report tree is hardcoded. Needs real budget-vs-actual report data, date/fund filters, export/open-history behavior, and drilldown. | `AlternateBudgetVsActualPanel` | Budget report service; report generation/export services; date range context | Needed: report service tests and UI guard tests for empty/service-error state. |
| `ASSETS_REGISTER` | `PanelHost.DefaultPanelFactory` -> `AssetsRegisterPanel` | Demo-only | `InventoryPanelFX`; depreciation run workflows | Current asset rows are hardcoded and save/delete only update local table state. Needs real asset register queries, asset CRUD, depreciation linkage, and audit-safe persistence. | `AlternateAssetsRegisterPanel` | Asset/inventory service; depreciation services; audit/result services | Needed: asset service tests and guard tests that no demo assets load by default. |
| `DEPRECIATION_RUNS` | `PanelHost.DefaultPanelFactory` -> `DepreciationRunsPanel` | Mixed / mostly service-backed | `InventoryPanelFX` depreciation flow; depreciation service classes | Has real lifecycle/processing service calls for draft/calculate/lock/unlock/delete and journal preview hints, but UI still needs stronger active-company context, result summaries, rollback/error handling, and integration with the asset register. | `AlternateDepreciationRunsPanel` | `DepreciationRunLifecycleService`; `DepreciationRunProcessingService`; asset services; journal/audit services | Partial: existing service coverage should be retained/expanded; add UI boundary tests for invalid periods, company gating, and status transitions. |
| `REPORT_LIBRARY` | Deprecated alias normalized by `MainWindowAlternate.openPanel()` to `REPORTS_WORKSPACE`; `PanelHost.DefaultPanelFactory` handles alias with `ReportLibraryPanel` | Legacy adapter / compatibility alias | `ReportsPanelFX`; `GenerateReportPanelFX`; semantic report preview panels and report writers | Alias should remain only for cross-branch compatibility until references are removed. No separate native workflow should be built for this ID. | None; retire alias after callers migrate to `REPORTS_WORKSPACE` | Same as reports workspace | Needed: routing compatibility test only while alias exists. |
| `REPORTS_WORKSPACE` | `PanelHost.DefaultPanelFactory` -> `ReportLibraryPanel` wrapping `ReportsPanelFX` | Legacy adapter | `ReportsPanelFX`; `GenerateReportPanelFX`; `SemanticReportPreviewPanelFX`; report writer/actions | Needs native report catalog, report run configuration, preview, export, open history, scheduled runs, and consistent date/fund/report context. | `AlternateReportsWorkspacePanel` | Report generation services; semantic report services; export services; scheduled report service | Needed: service-level report tests; adapter smoke test; native workflow tests when migrated. |
| `CHART_OF_ACCOUNTS` | `PanelHost.DefaultPanelFactory` -> `ChartOfAccountsPanel` | Mixed / read-only service-backed placeholder | `AccountsPanelFX`; `CoaEditorPanelFX`; `ChartOfAccountsTablePanelFX`; `ExportCoaXlsxActionFX`; `ImportCoaXlsxActionFX` | Current alternate panel lists active posting accounts via `AccountLookupService` but add/edit/delete/deactivate and COA import/export are not built. Placeholder suggests seed data when empty. | `AlternateChartOfAccountsPanel` | `AccountLookupService`; account maintenance service; COA import/export services; validation policy for duplicate/history changes | Partial: add read-only listing tests if missing; add service/UI tests for maintenance and import/export when implemented. |
| `DATABASE_ADMIN` | `PanelHost.DefaultPanelFactory` -> `AlternateDatabaseAdminPanel`; command center exposes database commands | Native / partial | `DatabaseOpenService`; `H2SchemaMigrator`; database open/import/export call sites | Open, close, import, export/backup, validate, repair/recover, and migrate schema are now presented as distinct native operations. File-picker polish and deeper H2 validation can still improve. | `AlternateDatabaseAdminPanel` | `AlternateDatabaseAdminService`; `DatabaseOpenService`; `H2SchemaMigrator` | Added service tests for invalid path, unsupported extension, repair failure/safety, export target exists, command availability, and open-after-import/open-after-repair boundary behavior. |
| `COMPANY_ADMIN` | `PanelHost.DefaultPanelFactory` -> `AlternateCompanyAdminPanel`; command center exposes company commands | Native / partial | `CreateOrEditCompanyPanelFX`; `CreateOrEditCompanyActionFX`; `CompanySelectionPanelFX`; `OpenCompanyFileActionFX`; `CreateOrEditCompanyPanelFXTest` | Native workflow now lists, creates, opens/switches, closes, deletes inactive companies with exact-name confirmation and backup acknowledgement, populates starter chart/settings idempotently, and creates deterministic sample companies only by explicit action. Further polish can add richer edit support and file-picker backup integration. | `AlternateCompanyAdminPanel` | `AlternateCompanyAdminService`; `AlternateDataContextService`; `UiSessionContext`; `CompanyRepository`; `CurrentCompany` compatibility bridge | Added service workflow tests for duplicate names, invalid required fields, create/open/close/delete/populate/sample workflows, active-company delete prevention, and populate already-populated behavior. |
| `IMPORT_EXPORT` | `WorkspaceRouter` routes to `PanelHost.DefaultPanelFactory` -> `AlternateImportExportPanel`; command center opens the native workspace | Native / partial | `SclxImportPanelFX`; `ImportSclxActionFX`; SCLX services/tests; `ImportCoaXlsxActionFX`; `ExportCoaXlsxActionFX`; database import/export/backup code; spreadsheet/XLSM/JSON/banking import/export actions where supported | Native workspace separates database-level operations from company-level COA/SCLX/other-format operations, defines preview/validate/commit/new-database import modes and active-company/database/COA/SCLX export modes, and reports standard created/updated/skipped/warnings/errors counts. COA and non-mutating SCLX targets still need deeper service implementation before full commits/previews are enabled. | `AlternateImportExportPanel` | `AlternateImportExportService`; `SclxImportService`; `SclxImportOptions`; `SclxImportResult`; `NonprofitBookkeepingSclxImportTarget`; COA import/export services; database admin service | Added command availability/routing updates and service boundary tests for SCLX count normalization and blocking errors. |
| `FUNDS` | `PanelHost.DefaultPanelFactory` -> `FundsPanel` | Native / partial | `FundsPanelFX`; fund accounting legacy workflows | Native workspace lists funds with ledger-derived balances, prevents direct balance editing, posts fund restriction reclassification journal entries, distinguishes reclassification from bank-account movement, and drills down to selected-fund ledger transactions. Further polish can connect a normalized fund maintenance table when the legacy/current-company model grows explicit active fund metadata. | `FundsPanel` / future `AlternateFundsPanel` split if desired | `AlternateFundsService`; `LedgerTransactionQueryService`; active `CurrentCompany` ledger/journal; chart of accounts | Added service tests for generated journal entries, selected-fund drilldown, and ledger-derived balance display. |
| `SETTINGS` | `PanelHost.DefaultPanelFactory` creates native `org.nonprofitbookkeeping.ui.SettingsPanel` | Native / mixed | `SettingsPanelFX`; `SettingsService`; `PreferencesService`; account lookup services | Native panel separates app, database, company, and report settings; lifecycle/destructive actions are linked to admin workspaces. Company-level persistence still depends on an open database/company context and the legacy database-backed `SettingsService`. | `SettingsPanel` | `SettingsService`; `PreferencesService`; `UiSessionContext`; `AccountLookupService` | Added validator tests for fiscal year/account validation. Add database-backed UI/service integration coverage as settings storage is further decoupled. |

## Required alternate workflows not yet represented by dedicated AppPanelId values

| Workflow | Current class or route | Current status | Old UI / existing source to inspect | Missing old functionality / migration notes | Target native panel or workflow | Required services / collaborators | Test status |
|---|---|---|---|---|---|---|---|
| Company administration | `COMPANY_ADMIN`; `PanelHost.DefaultPanelFactory` -> `AlternateCompanyAdminPanel` | Native / partial | `CreateOrEditCompanyPanelFX`; `CreateOrEditCompanyActionFX`; `CompanySelectionPanelFX`; `OpenCompanyFileActionFX`; `CloseCompanyFileAction`; `SaveCompanyFileAction`; `CreateOrEditCompanyPanelFXTest` | Create/list/open/switch/close/delete/populate/sample are first-class in the alternate workspace. Delete is intentionally defined as removing only the selected `company_store` row from the active database, never deleting the database file; active-company deletion is blocked until the context is closed/switched. Edit-company details and integrated database-export picker remain future polish. | `AlternateCompanyAdminPanel` | `AlternateCompanyAdminService`; `AlternateDataContextService`; current-company compatibility bridge; sample/populate service; backup/export guidance | Added workflow tests for duplicate names, invalid fields, open/switch/close, delete active company prevention, delete confirmation/backup, populate/sample, and populate already-populated. |
| Database administration | `DATABASE_ADMIN`; `PanelHost` native route and command-center database commands | Native / partial | `DatabaseOpenService`; `AlternateDataContextService`; `OpenCompanyFileActionFX`; H2 migration/action notes | Native panel now distinguishes open, close, import, export/backup, validate, repair/recover, and migrate schema, including source/target/backup/result summaries and exclusive active-context protection for repair/migration. | `AlternateDatabaseAdminPanel` | `AlternateDatabaseAdminService`; database open/admin service; recents store; backup/export service; validation/migration services | Added tests for invalid path, unsupported extension, export target exists, repair failure/safety, command availability, and open-after-import/open-after-repair boundary behavior. |
| Import/export hub | `IMPORT_EXPORT`; command center import/export group | Missing / scattered | `ImportFileActionFX`; `ExportFileActionFX`; `InputFileActionFX`; `OutputFileActionFX`; SCA ledger import actions; COA import/export actions; SCLX action/panel | No unified alternate import/export workspace. Needs clear mode labels for database, company, chart of accounts, SCLX, spreadsheet/XLSM/JSON where supported, plus preview/validate/commit/result-summary behavior. | `AlternateImportExportPanel` or `IMPORT_EXPORT` route | Import/export service family; admin operation safety DTOs; file chooser/validation support; result summaries | Needed: service tests for each supported mode and UI boundary tests for validation/commit gating. |
| SCLX import | Legacy `SclxImportPanelFX` / `ImportSclxActionFX`; no alternate route | Missing in alternate shell | `SclxImportPanelFX`; `ImportSclxActionFX`; `SclxImportService`; `SclxImportOptions`; `SclxImportResult`; `SclxImportTarget`; `NonprofitBookkeepingSclxImportTarget`; `SclxParser` | Needs alternate UI wrapper/workflow that uses existing SCLX parser/service/target classes, supports preview/validation/options, and commits only after blocking errors are resolved. | `AlternateSclxImportPanel` or section in `AlternateImportExportPanel` | Existing SCLX import services and result types; active company target; duplicate/import mode policies | Partial service coverage exists; add UI/service-boundary tests and reuse existing SCLX tests. |
| Chart-of-accounts import/export | COA buttons/actions exist in legacy actions; no alternate route except read-only `CHART_OF_ACCOUNTS` | Missing / partially adjacent | `ExportCoaXlsxActionFX`; `ImportCoaXlsxActionFX`; `CoaEditorPanelFX`; `AccountsPanelFX`; `ChartOfAccountsTablePanelFX` | Alternate chart panel does not expose COA import/export, duplicate-code policy, account deactivation policy, or history-safe edit rules. | COA import/export section in `AlternateChartOfAccountsPanel` or `AlternateImportExportPanel` | COA import/export services; account maintenance/validation services; result DTOs | Needed: tests for duplicate codes, invalid files, deactivation/history policies, export target exists. |
| Database repair / recovery | Section in `AlternateDatabaseAdminPanel`; database command-center actions route to the native administration workspace | Native / partial | `H2SchemaMigrator`; `scripts/migrate_h2_schema.py`; `H2SchemaMigratorTest`; `DatabaseOpenService` | Native workflow requires explicit backup confirmation before repairing the active database, closes active context for exclusive repair/migration, and reports source/backup/result paths. | Repair/recovery section in `AlternateDatabaseAdminPanel` | `AlternateDatabaseAdminService`; `H2SchemaMigrator`; database admin/backup service; validation/migration services | Added workflow tests for invalid path, repair failure, target exists, backup/confirmation requirements, and open-after-repair boundary behavior. |
| Documents & Attachments | Command center currently exposes `Documents & Attachments` via `MainWindowAlternate.openDocumentsDirect()` and `DefaultBankingPanelFactory.createDocumentsPanel()` wrapping `DocumentsPanelFX` | Dropped/not in scope | `DocumentsPanelFX`; `DocumentStorageService` | The active alternate UI migration plan explicitly drops Documents & Attachments. Do not migrate or expand this workflow as part of the alternate UI plan. Existing command-center exposure should be treated as legacy carryover and a candidate for removal/deferral from migration scope. | None | None for this plan | Not applicable. |

## Immediate documentation findings

1. All `AppPanelId` values are represented in `PanelHost.DefaultPanelFactory`, but several are normally bypassed by `WorkspaceRouter` and handled by custom panes in `MainWindowAlternate` (`DASHBOARD`, `BUDGET_EDITOR`, and `SCHEDULES`); `SETTINGS` now uses `PanelHost`.
2. The alternate shell still contains production-path hardcoded accounting values in dashboard, ledger, budget, budget-vs-actual, schedules fallback accounts, and assets workflows. These should be addressed by the separate no-demo-data plan step.
3. Company, database, import/export, SCLX import, COA import/export, and database repair are present only as partial/scattered flows or are missing dedicated alternate workflow IDs.
4. Documents & Attachments must remain dropped/not in scope despite the current command-center legacy adapter entry.
