package org.nonprofitbookkeeping.ui;

/** Domains exposed by alternate UI global search. */
public enum SearchResultType
{
    ACCOUNT,
    TRANSACTION,
    FUND,
    DONOR,
    REPORT,
    COMPANY,
    IMPORT_EXPORT_HISTORY
}
