/**
 * Defines the public integration API for nonprofit bookkeeping.
 *
 * <p>The API package is the stable surface for external callers and extension points that need predictable contracts across releases.</p>
 *
 * <p>Responsibilities:</p>
 * <ul>
 *   <li>Expose stable interfaces for integrations.</li>
 *   <li>Isolate consumers from internal implementation details.</li>
 *   <li>Support long-lived compatibility expectations.</li>
 * </ul>
 *
 * @since 1.0
 */
package nonprofitbookkeeping.api;
