/**
 * Persistence repositories for operational record types that originated in legacy
 * import/export workflows and are now treated as first-class persisted entities.
 */
package nonprofitbookkeeping.persistence.records;
